services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672" # AMQP Protocol
      - "15672:15672" # Web UI
    environment:
      RABBITMQ_DEFAULT_USER: galarza
      RABBITMQ_DEFAULT_PASS: 12413087
